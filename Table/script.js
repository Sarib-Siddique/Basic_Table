var userinput = +prompt("Enter Table Value");
var userinput2 = +prompt("Enter Table Value");

for (var i = 1; i <= userinput2; i++) {
  document.write(userinput + " x " + i + " = " + userinput * i, "<br>");
}
